"""Entry point for the demo service: ``python app.py``.

The service itself lives in ``dejasolve/service.py``; this file only keeps the
command short.
"""

from dejasolve.service import main

if __name__ == "__main__":
    main()
